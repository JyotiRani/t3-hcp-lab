apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  namespace: logistics
  labels:
    app: tracking-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      containers:
      - name: tracking-service
        image: gandigit/logistics-linux-tracking:latest  # Replace with your Docker registry
        imagePullPolicy: Always
        ports:
        - containerPort: 8004
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: logistics-secrets
              key: DATABASE_URL
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: logistics-secrets
              key: JWT_SECRET_KEY
        - name: JWT_ALGORITHM
          valueFrom:
            configMapKeyRef:
              name: logistics-config
              key: JWT_ALGORITHM
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: logistics-config
              key: AUTH_SERVICE_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: logistics
  labels:
    app: tracking-service
spec:
  type: ClusterIP
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP
    name: http
  selector:
    app: tracking-service

# Made with Bob
